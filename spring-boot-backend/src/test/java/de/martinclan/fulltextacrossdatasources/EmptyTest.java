package de.martinclan.fulltextacrossdatasources;

import org.junit.jupiter.api.Test;

public class EmptyTest {
    /***
     * This test is used to download all dependencies for jUnit tests without actually invoking Spring testing and as a
     * sanity check that jUnit is configured correctly.
     */
    @Test
    public void emptyTest() {
        assert(true);
    }
}
